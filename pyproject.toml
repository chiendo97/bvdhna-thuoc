[project]
name = "bvdhna-thuoc"
version = "0.1.0"
description = "Static PDF viewer for GitHub Pages"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pypdf",
    "pymupdf",
    "python-docx",
    "playwright",
    "pillow>=12.1.0",
]
